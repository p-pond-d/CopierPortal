# 📚 ภาพรวมสถาปัตยกรรมระบบ Copier Portal Dashboard

ระบบ **Copier Portal** เป็นเว็บแอปพลิเคชันที่ถูกพัฒนาขึ้นเพื่อการบริหารจัดการและวิเคราะห์ข้อมูลรายงานการใช้งานเครื่องถ่ายเอกสารและเครื่องพิมพ์ (Copier & Printer Usage Reports) โดยอัตโนมัติ ระบบถูกออกแบบมาให้รองรับการนำเข้าไฟล์รายงานจากเครื่องถ่ายเอกสาร (CSV/Excel) และนำมาประมวลผลเป็นกราฟสถิติ เปรียบเทียบข้อมูลรายปี (YoY) พร้อมทั้งคำนวณค่าใช้จ่ายตามอัตราค่าบริการที่กำหนดได้อย่างแม่นยำ

นอกจากนี้ยังให้ความสำคัญกับกฎหมายคุ้มครองข้อมูลส่วนบุคคล (PDPA) ด้วยระบบ **Privacy Mode** เพื่อป้องกันการเข้าถึงข้อมูลรายบุคคลโดยไม่ได้รับอนุญาต

---

## 🛠️ เทคโนโลยีที่ใช้ (Tech Stack)

### Frontend (ระบบหน้าบ้าน)
- **Framework:** React.js
- **UI/UX Design:** Bootstrap ผสมผสานกับการออกแบบสไตล์ **Glassmorphism** (ความโปร่งแสงแบบกระจก) ให้ความรู้สึกทันสมัยและหรูหราแบบ Premium
- **Data Visualization:** Chart.js สำหรับแสดงผลกราฟสถิติ แท่ง (Bar Chart) และเส้น (Line Chart) ที่มีการโต้ตอบได้
- **File Processing:** `xlsx` (SheetJS) สำหรับอ่านและประมวลผลไฟล์ Excel/CSV ก่อนส่งให้หลังบ้าน

### Backend (ระบบหลังบ้าน)
- **Framework:** Node.js พร้อมด้วย Express.js
- **Authentication:** JSON Web Token (JWT) สำหรับระบบ Login และการจัดการสิทธิ์การเข้าถึงแบบ Role-based
- **API Architecture:** RESTful API แยกการจัดการเป็นระบบ Route ต่างๆ (Auth, Reports, Users, Rates, Logs)

### Database & Deployment (ฐานข้อมูลและการติดตั้ง)
- **Database:** PostgreSQL (Host บน Neon Cloud Database) พร้อมใช้ไลบรารี `pg` ในการเชื่อมต่อ
- **Deployment:** Vercel (CI/CD) ทำหน้าที่โฮสต์ทั้ง Frontend และ Serverless Backend Functions อัตโนมัติเมื่อมีการ Push Code ไปยัง GitHub

---

## 🌟 ฟีเจอร์หลักของระบบ (Core Features & Modules)

### 1. Dashboard (ภาพรวมระบบ)
แสดงสรุปผลการใช้งานประจำเดือนและสถิติรายปี ข้อมูลรวมเช่น ยอดพิมพ์ทั้งหมด (B&W/Color), สแกนเนอร์, ยอดผู้ใช้งาน และรวมค่าบริการสุทธิ พร้อมกราฟแท่งและกราฟเส้นเพื่อวิเคราะห์แนวโน้มการใช้งาน

### 2. ข้อมูลรายบุคคล (User Data Drill-down)
สามารถค้นหาและดูประวัติการพิมพ์ของแต่ละบุคคลได้ (หากเข้าสู่ระบบด้วยสิทธิ์ผู้ดูแลระบบ) ระบบจะสรุปว่าพนักงานคนนี้มียอดใช้งานปริมาณเท่าใดและคิดเป็นเงินเท่าใด พร้อมสามารถนำออกเป็น CSV ได้

### 3. แยกตามประเภท (Categorized Breakdown)
แสดงกราฟแบบ Stacked Bar Chart แยกตามประเภทการใช้งาน (Print ขาวดำ, Print สี, Copy ขาวดำ, Copy สี, Scan) เปรียบเทียบเป็นรายเดือนแบบปีต่อปี (Year-over-Year) และเป็นพื้นที่สำหรับตั้งค่า **อัตราค่าบริการ (Rate Settings)**

### 4. นำเข้าข้อมูล (Data Import)
โมดูลสำคัญที่ใช้อ่านไฟล์จากเครื่องถ่ายเอกสาร (เช่น ไฟล์ที่ดึงมาจาก Web Interface ของเครื่อง Copier) 
- รองรับการลากวางไฟล์ (Drag & Drop)
- มีระบบวิเคราะห์ชื่อไฟล์อัตโนมัติเพื่อเชื่อมโยงเข้ากับเครื่องพิมพ์ในคลัง
- ระบบป้องกันการอัปโหลดซ้ำ (Duplicate Detection) และจัดการข้อขัดแย้ง (Conflict Resolution) หากตรวจพบการอัปโหลดข้อมูลของเดือนเดียวกัน

### 5. คลังเครื่องพิมพ์ (Printers Inventory)
ระบบจัดการทรัพย์สิน (Asset Management) สำหรับจัดเก็บรายชื่อเครื่องถ่ายเอกสารในองค์กร พร้อมหมายเลขซีเรียลและสถานที่ตั้ง เพื่อใช้ในการผูกข้อมูลเข้ากับรายงานการใช้งาน

### 6. จัดการผู้ใช้งาน (User Management)
กำหนดสิทธิ์การเข้าถึงระบบ โดยแบ่งเป็น:
- **Admin (ผู้ดูแลระบบ):** มองเห็นทุกเมนู, สามารถนำเข้าข้อมูล, แก้ไขอัตราค่าบริการ, จัดการผู้ใช้ และดูข้อมูลดิบได้
- **User (ผู้ใช้งานทั่วไป):** ถูกจำกัดสิทธิ์ให้ดูได้เฉพาะภาพรวมข้อมูล โดยระบบจะบังคับเปิดใช้งาน **Privacy Mode** ตลอดเวลา

### 7. บันทึกเหตุการณ์ (System Logs)
ระบบ Audit Trail ที่เก็บบันทึกทุกการกระทำสำคัญ เช่น การนำเข้าไฟล์ใหม่, การลบไฟล์, การแก้ไขอัตราค่าบริการ, การเพิ่มผู้ใช้ เพื่อให้ตรวจสอบความโปร่งใสย้อนหลังได้

### 8. Privacy Mode (PDPA Compliance)
โหมดหน้ากากสำหรับปกปิดข้อมูลส่วนตัว (Data Masking)
- ปิดบังชื่อจริง (เช่น `p****a`)
- ปิดบัง User ID (เช่น `8****9`)
- ช่วยให้นำเสนอหน้าจอ Dashboard ในที่สาธารณะหรือรายงานต่อผู้บริหารได้โดยไม่ละเมิดข้อมูลส่วนบุคคล

---

## 🗄️ โครงสร้างฐานข้อมูล (Database Schema)

ระบบถูกออกแบบด้วยฐานข้อมูลเชิงสัมพันธ์ (Relational Database) มีตารางสำคัญดังนี้:

1. **`Users`**: จัดเก็บข้อมูลผู้ดูแลและบัญชีผู้ใช้ระบบ (เข้ารหัสผ่านด้วย bcrypt)
2. **`Reports`**: ข้อมูล Metadata ของไฟล์รายงานที่ถูกอัปโหลด (ชื่อไฟล์, วันที่ประจำเป็นงวด, ชื่อเครื่องพิมพ์, รวมยอดเงิน)
3. **`UsageDetails`**: ข้อมูลรายละเอียดบรรทัดต่อบรรทัดที่สกัดจากไฟล์ CSV (1 แถว = การใช้งานของ 1 User ต่อรอบเดือนนั้นๆ) 
4. **`MonthlySummaries`**: ตาราง Aggregation สรุปยอดรวมระดับเดือน (Cache Table) เพื่อให้หน้า Dashboard โหลดได้รวดเร็วที่สุดโดยไม่ต้อง Sum ข้อมูลนับล้านแถวใหม่ทุกครั้ง
5. **`Printers`**: ทะเบียนคลังเครื่องพิมพ์
6. **`SystemLogs`**: บันทึกกิจกรรมระบบเพื่อวัตถุประสงค์ในการตรวจสอบ (Audit)

*(หมายเหตุ: **อัตราค่าบริการ** ในปัจจุบันถูกจัดเก็บแบบ Serverless Temporary State ผ่านไฟล์ `/tmp/rates.json` สำหรับการใช้งานบน Vercel และ `config/rates.json` บน Local Environment)*

---

## 🔄 กระบวนการทำงาน (Workflow) ทั่วไป
1. ผู้ดูแลระบบทำการดึงไฟล์สรุปยอดรายเดือนออกมาจากเครื่องถ่ายเอกสาร (Copier) เป็นสกุล `.csv`
2. ผู้ดูแลระบบนำไฟล์มาโยนใส่เมนู **"นำเข้าข้อมูล"** ในระบบ Copier Portal
3. Backend นำไฟล์ไปประมวลผล สกัดชื่อคอลัมน์ (เช่น Print B&W, Print Full Color) และจับคู่เข้ากับตารางในฐานข้อมูล 
4. ระบบจะทำการสรุปยอดรวม (Aggregation) แล้วจัดเก็บลงตาราง `MonthlySummaries` เพื่อเตรียมส่งออกแสดงผลทันที
5. ผู้บริหารสามารถเปิดดู **Dashboard** และสรุปรายงาน **แยกตามประเภท** เพื่อดูแนวโน้มค่าใช้จ่ายและทำ Budgeting ในปีต่อไปได้อย่างมีประสิทธิภาพ
